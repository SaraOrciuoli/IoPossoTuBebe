@model Admin.Models.CustomModel.ModelCompleta

@{
    ViewData["Title"] = "Menu";
    Layout = "~/Views/Shared/_LayoutServizioTavoli.cshtml";
}
<div class="row">
    <div class="col-xs-12">
        <div class="text-center">
            <h2 class="text-center"><strong>Riepilogo Dati Ordine</strong></h2>
        </div>
    </div>
</div>

<div class="container-fluid">
    @using (Html.BeginForm("SalvaOrdine", "ServizioTavoli", FormMethod.Post, new { enctype = "multipart/form-data", onsubmit = "validateForm(event)" }))
    {
        @Html.HiddenFor(model => model.totale);
        @Html.HiddenFor(model => model.TotaleConServizio);
        @Html.HiddenFor(model => model.MetodoPagamento);
        @Html.HiddenFor(model => model.Resto);
        @Html.HiddenFor(model => model.Ordine.Nominativo);
        @Html.HiddenFor(model => model.Ordine.Tavolo);
        @Html.HiddenFor(model => model.Ordine.Indirizzo);
        @Html.HiddenFor(model => model.Ordine.Citta);
        @Html.HiddenFor(model => model.Ordine.Provincia);
        @Html.HiddenFor(model => model.Ordine.Cap);
        @Html.HiddenFor(model => model.Ordine.NumTelefono);


        <!--<div class="row-co-first d-flex">
            <h4><strong>Prodotti:</strong></h4>-->
        @*<p><strong>Totale: <span class="color-org"> @Model.TotaleConServizio €</span></strong></p>*@
        <!--</div>-->
        <div class="row text-center Margin-Top">
            <div class="col-xs-12 panelProdottiTavoli">
                <div id="ListProduct">

                </div>
            </div>
        </div>
        <br />
        @*<div class="row">
                <div class="col-xs-12">

                    <div class="col-xs-12 text-center no-margin-left ">
                        <h3><strong>Paga in cassa:</strong></h3>
                    </div>


                    @if (@System.Configuration.ConfigurationManager.AppSettings["MetodiPagamento"] == "1")
                    {
                        <div class="col-xs-12 text-center">
                            <div class="form-check radio-inline">
                                <input class="form-check-input" type="radio" name="inlineRadioOptions" id="inlineRadio1" value="Conanti" onchange="ShowInput()">
                                <label class="form-check-label" for="inlineRadio1" style="font-size:18px">Paga in Contanti</label>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="col-xs-12 text-center" style="padding: 0px;">
                            <div class="form-check  metodoPagamento " style="background-color: #373736" onclick="MetodoSelezionato()">
                                <input class="form-check-input " style="display:none;" type="radio" name="inlineRadioOptions" id="inlineRadio1" value="Conanti" onchange="ShowInput()">
                                <label class="form-check-label " for="inlineRadio1" style="font-size:18px;"><img style="width: 45px; padding: 1vw 2vw;" src="~/images/contanti.png" /><em>Paga in Contanti</em></label>
                            </div>
                            <div class="form-check metodoPagamento" style="margin-top: 10px; background-color:#373736" onclick="MetodoSelezionato()">
                                <input class="form-check-input" type="radio" style="display:none;" name="inlineRadioOptions" id="inlineRadio2" value="Bancomat" onchange="ShowInput()">
                                <label class="form-check-label " for="inlineRadio2" style="font-size: 18px; padding-right: 25px;"><img style="width: 40px; padding: 1vw 2vw;" src="~/images/carta.png" /><em>Paga con Carte</em></label>
                            </div>
                        </div>


                    }

                    <div class="col-xs-12 text-center no-margin-left ">
                        <h3><strong>Paga in app:</strong></h3>
                    </div>
                    <div class="text-center ">
                        <img class="metodoPagamento" src="~/images/paypal-logo.png" />
                    </div>

                </div>
            </div>*@


        <!--<div class="row">
            <div id="DivVersatoCliente" class="col-xs-12 text-right">-->
        @*<h4>@Vetrina.App_GlobalResources.labels.Completa_resto</h4>*@
        @*<input   type="text" id="versato" onchange="ModResto()" />*@
        <!--<input   type="number" id="versato" onchange="ModResto()" placeholder="Scrivi la cifra che inserisci per un eventuale resto.">
            </div>
        </div>-->
        <div class="row text-center" style="border-top: 1px solid #ddd">
            @*<div class="col-xs-12 ">
                    <h2><strong>Sub-Totale: <span class="color-org">@Model.totale €</span></strong></h2>
                </div>*@
            @*@if (Model.MostraServizio)
                {
                    <div class="col-xs-12 ">
                        <h2><strong>Servizio: <span class="color-org">@Model.CostoServizio €</span></strong></h2>
                    </div>
                    <div class="col-xs-12 ">
                        <h2><strong>Totale: <span class="color-org">@Model.TotaleConServizio €</span></strong></h2>
                    </div>
                }
                else
                {*@
            <div class="col-xs-12 ">
                @*<h2><strong>Totale: <span class="color-org">@Model.totale €</span></strong></h2>*@
            </div>
            @*}*@
        </div>
        if (!Model.MostraPulsante)
        {
            <div class="row">
                <div class="col-xs-12 text-center">
                    <h3 class="color-red">Quantità minima non raggiunta per effettuare un ordine</h3>
                </div>
            </div>
        }
        <div class="row text-center justify-content-Right">
            <div class="col-sm-4 col-xs-12 justify-content-Right ">

                <button class="btn btn-Special2" type="submit" value="modifica" name="buttonType">Modifica l'ordine</button>

            </div>
            <div class="col-sm-4 col-xs-12 justify-content-Right ">
                @if (Model.MostraPulsante)
                {
                    <button class="btn btn-Special2" type="submit" value="salva" name="buttonType">Completa l'ordine</button>
                }
            </div>
        </div>
    }
</div>

@*@(Html.DevExtreme().LoadPanel()
    .ID("loadPanel")
    .ShadingColor("rgba(0,0,0,0.4)")
    //.Position(p => p.Of("#employee"))
    .Visible(false)
    .ShowIndicator(true)
    .ShowPane(true)
    .Shading(true)
    )*@

<script>
      $(document).ready(function () {
       /*document.getElementById("DivVersatoCliente").style.visibility = "hidden";*/

        $('#ListProduct').dxList({
            dataSource:@Html.Raw(WebUtility.HtmlDecode(@Model.Prodotti)),
            itemTemplate(itemData, itemIndex, itemElement) {
                if (itemData.Quantita != 0) {
                    var uscita = itemData.OrdineUscita != "" || itemData.OrdineUscita != null || itemData.OrdineUscita != "0" ? itemData.OrdineUscita : " - ";
                    return $('<div class="outer-co-item row">')
                            .append($('<div class="co-left-col col-xs-8">' +
                                '<div class="order-co-name" style="white-space: normal;">' + itemData.Descrizione + '</div>' +
                                '<div class="order-co-quant order-co">' + "N° Prodotti: " + itemData.Quantita + '</div>' +
                                '<div class="order-co-price-single order-co">' + "Prezzo singolo: " + '<span class="color-red">' + itemData.Prezzo.toString() + "€" + '</span></div>' +
                            /*'<div class="order-co">' + "Portata n°: " + uscita + '</div>' +*/

                                '</div>' +
                                '<div class="co-right-col col-xs-4">' +
                                '<div class="picture-co-item" id="PathImg"><img class="picture-co" style="padding-top: 30px;" src=' + '@System.Configuration.ConfigurationManager.AppSettings["pathFoto"]'+ itemData.PathFoto + ' /></div>' +
                                '<div class="order-co-price">' + "Totale: " + '<span class="color-red">' + itemData.Calculate + "€" + '</span></div>' +
                                '</div>'));
                    }
            },
                pageLoadMode:'scrollBottom',
                earchEnabled: true,
                searchExpr: 'Descrizione',
            });
    });

/*   function ModResto() {
        var vers = document.getElementById("versato").value;
        document.getElementById("Resto").value = "Resto di " + vers + "€";
    }

    function ShowInput() {
        var Contanti = document.getElementById("inlineRadio1").checked;
        if (Contanti) {
            document.getElementById("DivVersatoCliente").style.visibility = "visible";
        }
        else {
            document.getElementById("DivVersatoCliente").style.visibility = "hidden";
        }
    }*/

@*    function validateForm(event) {
            var mostraBancomat = @Html.Raw(WebUtility.HtmlDecode(@Model.MostraBancomat));
            if (event.submitter.value == "salva") {
                var Contanti = document.getElementById("inlineRadio1").checked;
                if (mostraBancomat == "2") {
                    var Bancomat = document.getElementById("inlineRadio2").checked;
                    if (Contanti || Bancomat) {
                        if (Contanti) document.getElementById("MetodoPagamento").value = "Contanti";
                        if (Bancomat) document.getElementById("MetodoPagamento").value = "Carte";
                        var vers = document.getElementById("versato").value;
                        document.getElementById("Resto").value = "Resto di " + vers + "€";

                    }
                    else {
                        event.preventDefault();
                        alert("Selezionare un metodo di pagamento!");
                    }
                }
                else {
                    if (Contanti) {
                        if (Contanti) document.getElementById("MetodoPagamento").value = "Contanti";
                        if (Bancomat) document.getElementById("MetodoPagamento").value = "Carte";
                        var vers = document.getElementById("versato").value;
                        document.getElementById("Resto").value = "Resto di " + vers + "€";

                    }
                    else {
                        event.preventDefault();
                        alert("Selezionare un metodo di pagamento!");
                    }
                }

            }

    };*@
@*
    function getLoadPanelInstance() {
        return $("#loadPanel").dxLoadPanel("instance");
    }

    function addProduct(id) {
        getLoadPanelInstance().show();
        $.ajax({
           url: '@Url.Action("AddProdotto", "Shop")',
           type: 'POST',
           data: { idProd: id },
           success: function (idcat) {
               ShowProduct(idcat);
           },
           error: function () {
               alert("error");
           }
       });
     }



    function removeProduct(id) {
        getLoadPanelInstance().show();
        $.ajax({
           url: '@Url.Action("RemoveProdotto", "Shop")',
           type: 'POST',
           data: { idProd: id },
           success: function (idcat) {
               ShowProduct(idcat);
           },
           error: function () {
               alert("error");
           }
       });
    }*@

/*    function MetodoSelezionato() {
        const metodiPagamento = document.querySelectorAll(".metodoPagamento");

        metodiPagamento.forEach(div => {
            div.addEventListener("click", function () {
                this.classList.add("metodoPagamentoSelezionato");

                metodiPagamento.forEach(otherDiv => {
                    if (otherDiv !== this) {
                        otherDiv.classList.remove("metodoPagamentoSelezionato");
                    }
                });
            });
        });

    }*/
</script>


